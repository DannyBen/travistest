language: ruby

notifications:
  email: false

rvm:
- "2.5.0"

# before_install:
# - sudo apt-get update
# - sudo apt-get install python3 -y

addons:
  apt:
    sources:
    - deadsnakes
    packages:
    - python3.4


# install:
# - pip3 install codenamize

script: 
- ruby test.rb