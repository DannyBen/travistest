os:
- linux
- osx

language: crystal

script:
- crystal build hello.cr --release --static -o hello-$TRAVIS_OS_NAME

deploy:
  provider: releases
  api_key: $GITHUB_ACCESS_TOKEN
  skip_cleanup: true
  draft: true

  file:
  - hello-linux
  - hello-osx

  on:
    repo: DannyBen/travistest
